# Backend - Producción
# Copiá este archivo como .env.production y completá los valores

PORT=4000
# Cadena de conexión Atlas (formato SRV)
# Tip: reemplazá <user>, <pass>, <cluster>, <db> y opcionalmente appName
MONGO_URI=mongodb+srv://alanstoremongodb_db_user:9yHSH9msvrdCyGjQ@cluster0.4zdcyjo.mongodb.net/

# JWT (idealmente distintas de las de desarrollo)
JWT_ACCESS_SECRET=<genera-una-clave-larga-aleatoria>
JWT_REFRESH_SECRET=<genera-otra-clave-larga-aleatoria>
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# CORS: dominio del frontend en producción
CORS_ORIGIN=https://<tu-frontend.com>

# Importante para flags de cookies y carga del .env correcto
NODE_ENV=production

# Bootstrap opcional del primer usuario (solo si necesitás crearlo)
# Dejalo vacío si ya existe en la base nueva
ADMIN_USUARIO=
ADMIN_PASSWORD=
ADMIN_NOMBRE=Admin
